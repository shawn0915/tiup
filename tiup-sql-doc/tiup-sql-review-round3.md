# tiup sql 第三轮评审报告（最终汇总）

**评审人**：全能表妹（安全审计）
**评审时间**：2026-05-18
**评审范围**：`components/sql/` 全部源码及测试文件，`tiup-sql.md` 变更文档
**前置条件**：前两轮共发现 19+10 项问题，其中 10 项阻断/高/中已修复验证通过

---

## 审查结论

**代码安全合规，无阻断或高严重度问题。可以进入编译验证和集成测试阶段。**

---

## 本轮审查结果

### 1. 源码变更检查

自第二轮审查以来，无源码文件变更。文件时间戳确认：
- `executor.go` — May 18 12:45（第二轮修复时间，未再变更）
- `vertical.go` — May 18 12:45
- `config_test.go` — May 18 12:46
- 其余文件 — May 17 17:xx（原始开发时间）

### 2. 安全合规全面复查

| # | 检查项 | 状态 | 说明 |
|---|--------|------|------|
| 1 | 密码不以命令行明文传递 | ✅ | `--password` 为 bool flag |
| 2 | 密码交互式密文输入 | ✅ | term raw mode，退格正确 |
| 3 | TLS 证书验证默认启用 | ✅ | `InsecureSkipVerify: false` |
| 4 | `--tls-skip-verify` 需显式指定 | ✅ | Bool flag 默认 false |
| 5 | 元命令 SQL 注入防护 | ✅ | `isValidIdentifier()` + 反引号包裹 |
| 6 | 日志中密码脱敏 | ✅ | `SanitizedDSN()` + `sanitizeDSN()` |
| 7 | 连接配置文件权限 | ✅ | `0o600` |
| 8 | 连接配置名称安全 | ✅ | 非法字符过滤 |
| 9 | SQL 字符串内分号处理 | ✅ | 引号感知分割 |
| 10 | 环境变量密码风险已文档化 | ✅ | `tiup-sql.md` S1 节 |
| 11 | DSN 密码 URL 编码 | ✅ | `url.QueryEscape()` |
| 12 | Playground 实例发现 | ✅ | 复用 tiup 现有路径逻辑 |
| 13 | 跨平台密码输入 | ✅ | build tag 分离 unix/windows |
| 14 | REPL 信号处理 | ✅ | SIGINT/SIGTERM 正确处理 |
| 15 | TLS 配置正确注册 | ✅ | `mysql.RegisterTLSConfig()` |

### 3. 已知低优先级遗留项

以下为后续迭代可处理的项目，**不阻塞当前进度**：

| 编号 | 严重度 | 描述 | 影响 |
|------|--------|------|------|
| L1 | Low | 全局 `flags` 变量影响测试隔离 | 可测试性 |
| L2 | Low | `execQuery()` 统一用 `Query()` 而非区分 `Exec()` | 性能 |
| L3 | Low | `\i` 元命令不支持目录路径 | 功能一致性 |
| L4 | Low | `formatValue()` 对二进制 `[]byte` 可能乱码 | 显示 |
| L5 | Low | 测试覆盖仍不足（repl/cmd/tls/pool 无测试） | 工程质量 |
| L6 | Low | CJK 字符宽度对齐 | 国际化 |
| L7 | Low | CSV 缺少 BOM 头 | Excel 兼容性 |
| N1 | Low | `user:pass@host` 格式密码含 `@` 时解析失败 | 边缘兼容 |
| N2 | Low | `readStatements` 内二次调用 `splitStatements` 冗余 | 性能 |
| N3 | Low | `vertical.go` 的 `maxLabelLen` 仍每行计算 | 性能 |
| N4 | Low | MySQL `\'` 反斜杠转义未支持 | 边缘兼容 |
| N5 | Low | `MaxRows` 选项声明但未实现 | 功能缺失 |
| N6 | Low | 配置缓存无过期/失效机制 | 健壮性 |
| N7 | Low | TLS 注册名同 host:port 重复注册会冲突 | 边缘场景 |

### 4. 三轮评审累计统计

| 轮次 | 发现 | 已修复 | 未修复（低优先级） |
|------|------|--------|-------------------|
| 第一轮（@元芳） | 2 阻断 + 4 高 + 6 中 + 7 低 = 19 | 10（阻断+高+部分中） | 9（中+低） |
| 第二轮（@全能表妹） | 1 高 + 4 中 + 5 低 = 10 | 4（高+中） | 6（低） |
| 第三轮（@全能表妹） | 0 阻断 / 0 高 / 0 中 | 0 | 0 新增 |
| **合计** | **29** | **14** | **15 低优先级遗留** |

### 5. 设计文档符合度

| 模块 | 实现状态 | 评价 |
|------|----------|------|
| 连接管理 — DSN 解析 | ✅ 完整 | 多格式支持，URL 编码正确 |
| 连接管理 — TLS | ✅ 完整 | 证书验证、skip-verify、配置注册 |
| 连接管理 — Playground 发现 | ✅ 完整 | 复用 tiup 现有逻辑 |
| 连接管理 — 连接配置 | ⚠️ 基本完整 | 缺密码持久化和缓存失效 |
| 交互式 REPL | ⚠️ 核心功能完整 | 缺自动补全、语法高亮、历史持久化 |
| 批量执行 | ✅ 完整 | 引号感知、错误策略、目录扫描 |
| 输出格式化 | ✅ 完整 | 6 种格式，表格/CSV/JSON/TSV/垂直 |
| 日志与诊断 | ✅ 完整 | 查询日志、DSN 脱敏、慢查询提示 |
| 安全设计 | ✅ 合规 | 密码保护、TLS、SQL 注入防护、日志脱敏 |

### 6. 测试覆盖

| 包 | 测试文件 | 覆盖 | 测试用例数 |
|-----|---------|------|-----------|
| `connect` | `dsn_test.go` | DSN 解析、URL 编码、脱敏 | 8 |
| `connect` | `config_test.go` | 配置 CRUD、名称校验、文件创建 | 3 |
| `format` | `formatter_test.go` | 6 种格式输出、工厂方法 | 9 |
| `batch` | `executor_test.go` | 语句分割、字符串内分号 | 8 |
| `log` | `logger_test.go` | 日志记录、脱敏 | 3 |
| `repl` | 无 | — | 0 |
| `cmd` | 无 | — | 0 |
| `connect/tls` | 无 | — | 0 |
| **合计** | **5** | — | **31** |

---

## 最终建议

1. **立即**：进入编译验证（`go build ./components/sql/`）和集成测试
2. **编译通过后**：补充 `repl/`、`cmd/`、`connect/tls.go` 的单元测试
3. **后续迭代**：实现自动补全、语法高亮、历史持久化等高级功能

**@wdrhhwx 所有审查汇总完成，无阻断或高严重度问题，代码可以推进到编译验证和集成测试阶段。**
